Act as an Expert Full-Stack Developer specializing in real-time web applications using Vanilla HTML/JS, Tailwind CSS, and Supabase. 

I am providing you with the codebase for my current web application, "SecureQuiz". Currently, it is an asynchronous, highly secure examination platform with strict anti-cheat features (tab monitoring, fullscreen enforcement). 

**Your Objective:** I want you to completely refactor and rewrite this codebase to convert it into a synchronous, live multiplayer quiz game exactly like **Kahoot.it**.

Please read my provided files and generate the new code following these strict requirements:

### 1. Database Redesign (Supabase SQL)
Completely rewrite `supabase-schema.sql`.
* **Remove:** All anti-cheat metrics (tab switches, fullscreen exits, etc.) and the current `submissions` table.
* **Create `game_rooms` table:** Columns for `id`, `quiz_id` (references quizzes), `pin` (a unique 6-digit string), `host_id`, `status` (waiting, question_active, leaderboard, finished), and `current_question_index`.
* **Create `players` table:** Columns for `id`, `room_id`, `nickname`, and `total_score`.
* **Create `player_answers` table:** Columns for `id`, `player_id`, `room_id`, `question_index`, `is_correct`, `reaction_time_ms`, and `points_earned`.
* **Realtime & Security:** Enable Supabase Realtime for all these tables. Set up appropriate Row Level Security (RLS) policies (e.g., players can insert answers and read room status; hosts can update room status).

### 2. Architecture & State Management (JavaScript)
Rewrite the frontend logic to use **Supabase Realtime Channels** for synchronous gameplay. The game is entirely driven by the Host.
* **Host Logic:** The host creates a room, generates a PIN, and controls the flow (Start Game -> Show Question -> Time Up -> Show Leaderboard -> Next Question).
* **Player Logic:** The player joins via PIN. Their screen is a "dumb client" that only updates when the `game_rooms` status or `current_question_index` changes via Realtime subscriptions.
* **Scoring Logic:** Points should be awarded based on both correctness and speed (reaction time), just like Kahoot.

### 3. Split Frontend Views (HTML/JS)
Remove all existing proctoring, registration forms, and lockdown overlays. The UI must be split into two distinct user flows. You can handle this via dynamic DOM manipulation or separate HTML files.

**A. The Player View (Mobile-First)**
* Screen 1: Big input field for Game PIN.
* Screen 2: Input field for Nickname.
* Screen 3: "You're in! See your nickname on screen" (Waiting Lobby).
* Screen 4: Active Question. **Crucial:** The player screen must NOT show the question text or the answer text. It should ONLY display a 2x2 grid of massive buttons: 
   * Red Triangle (Top Left)
   * Blue Diamond (Top Right)
   * Yellow Circle (Bottom Left)
   * Green Square (Bottom Right)
* Screen 5: Result (Big green screen for "Correct!" + Points earned, or red screen for "Incorrect").

**B. The Host View (Desktop/Projector)**
* Screen 1: Lobby showing the massive Game PIN and a live-updating grid of player nicknames as they join.
* Screen 2: Active Question. Shows the timer counting down, the question text at the top, an optional image in the middle, and the 4 answers mapped to the Red, Blue, Yellow, and Green shapes at the bottom.
* Screen 3: Top 5 Leaderboard (Bar chart or podium style) shown after every question.

### 4. UI/UX Design (Tailwind CSS / Custom CSS)
* Completely strip away the dark, slate "hacker" theme (`bg-slate-950`, glassmorphism, etc.).
* Implement a bright, vibrant, gamified aesthetic. 
* Use stark, bold primary colors for the interactive elements (Red `#E21B3C`, Blue `#1368CE`, Yellow `#D89E00`, Green `#26890C`).
* Add playful CSS animations (e.g., pulsing buttons, slide-in transitions, bouncy score popups).
* Ensure the UI is highly responsive. The Player view must be perfectly optimized for tapping on mobile phones.

### Output Instructions
Please provide the updated code in the following order:
1. The completely new `supabase-schema.sql`.
2. The new `index.html` structure accommodating the Host and Player views.
3. The CSS styles (Tailwind classes + custom animations).
4. The JavaScript logic demonstrating the Supabase Realtime connection and game state management.